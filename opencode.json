{
    "$schema": "https://opencode.ai/config.json",
    "mcp": {
        "laravel-boost": {
            "type": "local",
            "enabled": true,
            "command": ["docker", "compose", "exec", "-T", "php-fpm", "php", "artisan", "boost:mcp"]
        }
    }
}
